apiVersion: v1
kind: Service
metadata:
  name: { { include "tcmautoresearch.fullname" . } }
  labels: { { - include "tcmautoresearch.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "tcmautoresearch.selectorLabels" . | nindent 4 } }
