# dev.txt — 开发 / 测试 / CI 依赖
# 包含 core.txt + 测试框架 + 代码质量工具。
# 安装：pip install -r requirements/dev.txt

-r core.txt

# ── 测试框架 ────────────────────────────────────────────────────
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=4.1,<6.0
pytest-mock>=3.12,<4.0
pytest-xdist>=3.5,<4.0
coverage>=7.4,<8.0

# ── 代码质量 / Lint ─────────────────────────────────────────────
black>=24.0,<25.0
flake8>=7.0,<8.0
mypy>=1.8,<2.0
pylint>=3.0,<4.0
isort>=5.13,<6.0
autopep8>=2.0,<3.0

# ── 安全扫描 ────────────────────────────────────────────────────
bandit>=1.7,<2.0
safety>=3.0,<4.0

# ── E2E 浏览器测试 ──────────────────────────────────────────────
selenium>=4.18,<5.0

# ── 构建 / 打包 ────────────────────────────────────────────────
setuptools>=69.0
wheel>=0.42
build>=1.2,<2.0
tox>=4.12,<5.0
